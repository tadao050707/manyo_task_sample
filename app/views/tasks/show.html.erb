<h1><%= t('.title') %></h1>

<p>
  <strong><%= Task.human_attribute_name(:title) %>:</strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:content) %>:</strong>
  <%= @task.content %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:created_at) %>:</strong>
  <%= @task.created_at.strftime('%Y/%m/%d %H:%M') %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:deadline_on) %>:</strong>
  <span><%= @task.deadline_on %></span>
</p>

<p>
  <strong><%= Task.human_attribute_name(:priority) %>:</strong>
  <span><%= t("enum.task.priority.#{@task.priority}") %></span>
</p>

<p>
  <strong><%= Task.human_attribute_name(:status) %>:</strong>
  <span><%= t("enum.task.status.#{@task.status}") %></span>
</p>

<p>
  <strong><%= t("activerecord.models.label") %>:</strong>
  <% @task.labels.each do |label| %>
    <span><%= label.name %></span>
  <% end %>
</p>

<%= link_to t('common.edit'), edit_task_path, id: 'edit-task' %> |
<%= link_to t('common.back'), tasks_path, id: 'back' %>
